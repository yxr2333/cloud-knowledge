package com.sheep.cloud.repository;

import com.sheep.cloud.model.ISafeAccountHistoryEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ISafeAccountHistoryEntityRepository extends JpaRepository<ISafeAccountHistoryEntity, Integer> {
}